moveSpeed = 2;
cooldownPeriod = 30; // How long a player has to wait before he/she may shoot again
currentCooldown = 0; // The current cooldown period
cooldownTick = 1; // How much the currentCooldown ticks down per update
currentHealth = 3;
keyPressed = false;

// Instantiate Global Variables

global.points = 0;
global.ppk = 20; // Points Per Kill
