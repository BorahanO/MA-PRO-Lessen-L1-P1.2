// OPDRACHT B:

depth = 10;

player = instance_create(room_width * 0.5, room_height * 0.75, obj_Player);


timer = 0;

// room_speed = Updates per seconde = 1 seconde
// x * room_speed = x * seconde
timerInterval = 0.5 * room_speed;

